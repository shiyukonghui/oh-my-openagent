# Open CoDesign 集成规划文档 - 第二次审查报告

**审查日期**: 2026-05-22
**审查文档**: [open-codesign-integration-plan.md](../../.plans/open-codesign-integration-plan.md)
**审查版本**: v0.1.0 (修正后)
**审查状态**: 大部分问题已修复，仍有少量遗留问题

---

## 审查摘要

| 类别 | 上次问题数 | 已修复 | 遗留 | 新增 |
|------|-----------|--------|------|------|
| 高优先级 | 5 | 5 | 0 | 0 |
| 中优先级 | 17 | 14 | 2 | 1 |
| 低优先级 | 1 | 1 | 0 | 0 |

**总体评价**: 文档质量显著提升，核心问题已修复，剩余问题为文档内部一致性问题。

---

## 已修复问题确认

### 高优先级问题（全部修复 ✅）

| # | 原问题 | 修复情况 |
|---|--------|---------|
| 1 | 品牌参考数量错误（28→25） | ✅ 已修复：1.2节、2.4节均已更新为 25 个 |
| 2 | 缺少许可证验证 | ✅ 已修复：1.3节添加了验证路径和说明 |
| 3 | API 依赖风险 | ✅ 已处理：文档明确"不引入 `@mariozechner/pi-*` 系列依赖" |
| 4 | 缺少回滚策略 | ✅ 已修复：新增第13节"回滚与降级策略" |
| 5 | Phase 1 任务编号错误 | ✅ 已修复：任务编号 1.1-1.10 连续完整 |

### 中优先级问题（大部分修复 ✅）

| # | 原问题 | 修复情况 |
|---|--------|---------|
| 6 | preview 工具实现方案模糊 | ✅ 已修复：7.3.3节添加了降级方案说明 |
| 7 | 工具命名不一致 | ✅ 已修复：统一使用 `codesign_*` 前缀 |
| 8 | 缺少性能影响评估 | ✅ 已修复：Phase 1.9 添加"Token 预算评估"任务 |
| 9 | 缺少测试策略 | ✅ 已修复：新增第14节"测试策略" |
| 10 | 技能注册机制不完整 | ✅ 已修复：5.1.3节说明通过 `agent` 字段自动过滤 |
| 11 | 缺少 OrchestratorContext 依赖说明 | ✅ 已处理：4.2节说明了与 Atlas 的差异 |
| 12 | 虚拟 FS vs 真实 FS 差异 | ✅ 已修复：11.2节添加了风险说明 |
| 13 | 模板资源路径问题 | ✅ 已处理：7.3.1节说明了模板位置 |
| 14 | 沙箱预览环境缺失 | ✅ 已处理：7.3.3节添加了降级方案 |
| 15 | 缺少错误处理策略 | ✅ 已修复：第13节添加了降级路径 |
| 16 | 缺少配置开关设计 | ✅ 已修复：新增第8节"配置开关设计" |
| 17 | 缺少版本兼容性说明 | ⚠️ 部分处理：未明确锁定版本 |

---

## 遗留问题

### 1. 品牌参考数量不一致（中优先级）

**位置**: 3.3节技能映射表、5.2节

**问题描述**: 
- 1.2节和2.4节已正确更新为"25 个品牌参考"
- 但 3.3节技能映射表仍写"品牌参考（28 个 DESIGN.md）"
- 5.2节仍写"将 28 个品牌 `DESIGN.md` 迁移"

**建议**: 全文搜索"28"并替换为"25"。

---

### 2. 配置开关设计与工具注册代码不一致（中优先级）

**位置**: 第8节 vs 7.2节 Step 7

**问题描述**: 
- 第8节明确说明"无需新增专属开关"，使用现有 `disabledAgents` 机制
- 但 7.2节 Step 7 的代码示例仍使用 `codesignEnabled` 变量：

```typescript
const codesignTools = codesignEnabled ? {  // ← 此变量不存在
  codesign_scaffold: createScaffoldTool(ctx),
  // ...
} : {}
```

**建议**: 更新代码示例，使用 `disabledAgents.includes("codesign")` 检查，或说明工具注册由 Agent 存在性自动控制。

---

### 3. Team Mode 注册路径不完整（中优先级）

**位置**: 9.3节

**问题描述**: 
- 文档提到需在 `AGENT_ELIGIBILITY_REGISTRY` 中注册
- 提供的路径 `src/features/team-mode/types.ts` 不完整，未给出具体代码位置

**验证结果**: `AGENT_ELIGIBILITY_REGISTRY` 确实存在于 `src/features/team-mode/types.ts`，但文档未给出具体注册代码示例。

**建议**: 补充具体注册代码示例：

```typescript
// src/features/team-mode/types.ts
export const AGENT_ELIGIBILITY_REGISTRY: Record<string, AgentEligibility> = {
  // ... 现有
  codesign: {
    eligible: true,
    teammate: "allow",
    role: "Designer / Frontend Prototyper",
  },
}
```

---

## 新增内容质量评估

### 第8节：配置开关设计 ✅

**评价**: 设计合理，复用现有 `disabledAgents` 机制，避免了新增配置复杂度。

**亮点**:
- 明确了配置优先级
- 说明了禁用后的效应（Agent 不出现、工具不注册、技能不注入）

### 第13节：回滚与降级策略 ✅

**评价**: 内容完整，覆盖了多种故障场景。

**亮点**:
- 渐进降级路径设计合理
- 故障排查提示实用

### 第14节：测试策略 ✅

**评价**: 测试范围明确，测试位置清晰。

**亮点**:
- 关键测试用例定义具体
- 测试位置与现有代码库结构一致

### 4.1节：温度策略改进 ✅

**评价**: 新增的"创造性策略"说明解决了温度与创造性的矛盾问题。

**亮点**:
- 温度固定 0.3，通过 prompt 引导创造性
- 明确了"默认模式"和"创意模式"的触发条件

### 6.2节：动态提示词构建改进 ✅

**评价**: 改为"构建时内联拼接"，避免了运行时文件系统读取。

**亮点**:
- 与 Atlas 的 prompt routing 模式一致
- 明确了各变体文件的职责

---

## 文档结构改进确认

| 改进项 | 状态 |
|--------|------|
| 新增第8节"配置开关设计" | ✅ |
| 新增第13节"回滚与降级策略" | ✅ |
| 新增第14节"测试策略" | ✅ |
| 目录更新（新增章节） | ✅ |
| Phase 1 任务编号修复 | ✅ |
| 工具命名统一为 `codesign_*` | ✅ |

---

## 最终建议

### 立即修复（实施前）

| # | 问题 | 修复方式 |
|---|------|---------|
| 1 | 3.3节、5.2节品牌数量仍为 28 | 全文搜索"28"替换为"25" |
| 2 | 7.2节 Step 7 代码示例使用不存在的变量 | 更新为 `!disabledAgents.includes("codesign")` 或删除条件判断 |

### 建议补充（可选）

| # | 问题 | 修复方式 |
|---|------|---------|
| 3 | Team Mode 注册代码示例缺失 | 补充具体注册代码 |

---

## 审查结论

**文档状态**: ✅ 可进入实施阶段（修复遗留问题后）

**修复工作量**: 约 15 分钟（仅需文本替换和代码示例更新）

**质量评分**: 8.5/10（较上次审查提升 3 分）

---

## 附录：验证记录

### 验证的方法

1. **全文阅读**: 逐节阅读修正后的文档
2. **数据验证**: 验证品牌数量、工具数量等关键数据
3. **代码库对照**: 对照现有代码库验证技术声明
4. **一致性检查**: 检查文档内部一致性

### 验证的关键文件

| 文件 | 验证内容 | 结果 |
|------|---------|------|
| `src/agents/types.ts` | `BuiltinAgentName` 类型定义 | ✅ 可扩展 |
| `src/agents/builtin-agents.ts` | `agentSources` 注册模式 | ✅ 模式正确 |
| `src/shared/model-requirements.ts` | `AGENT_MODEL_REQUIREMENTS` 结构 | ✅ 格式正确 |
| `src/features/builtin-skills/types.ts` | `BuiltinSkill` 接口 | ✅ 支持 `agent` 字段 |
| `src/features/team-mode/types.ts` | `AGENT_ELIGIBILITY_REGISTRY` | ✅ 存在 |

---

**审查完成日期**: 2026-05-22
**审查工具**: Trae AI Code Assistant
**下一步**: 修复遗留问题后可进入 Phase 1 实施